/**
 * Punto de entrada de los estilos: `import "nexora-ui/styles.css"`.
 *
 * Existe solo para que el build emita el CSS como un archivo aparte. Los componentes no
 * importan estilos por su cuenta, así que quien solo quiera los tipos no arrastra CSS.
 */
import "./index.css"
